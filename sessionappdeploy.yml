apiVersion: v1
kind: Service
metadata:
  name: sessionapp
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: sessionapp
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sessionapp
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  selector:
    matchLabels:
      app: sessionapp
  template:
    metadata:
      labels:
        app: sessionapp
    spec:
      containers:
        - name: sessionapp
          image: CONTAINER_IMAGE
          securityContext:
            privileged: false
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
          ports:
            - containerPort: 8080

